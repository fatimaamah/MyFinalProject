<div class="row mb-4">
  <div class="col-12">
    <h2><i class="bi bi-people me-2"></i>Level Coordinators</h2>
    <p class="text-muted">Manage all level coordinators</p>
  </div>
</div>

<div class="card shadow-sm">
  <div class="card-header bg-white d-flex justify-content-between align-items-center">
    <h5 class="mb-0"><i class="bi bi-list-ul me-2"></i>Coordinator List</h5>
  </div>
  <div class="card-body">
    <% if (coordinators && coordinators.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-hover align-middle">
          <thead class="table-light">
            <tr>
              <th>Full Name</th>
              <th>Email</th>
              <th>Level</th>
              <th>Department</th>
              <th>Created</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% coordinators.forEach(coordinator => { %>
              <tr>
                <td><%= coordinator.full_name %></td>
                <td><%= coordinator.email %></td>
                <td><%= coordinator.level || '—' %></td>
                <td><%= coordinator.department || '—' %></td>
                <td><%= new Date(coordinator.created_at).toLocaleDateString() %></td>
                <td>
                  <form action="/admin/delete-user/<%= coordinator.id %>" method="POST" style="display:inline;" onsubmit="return confirm('Delete this coordinator?')">
                    <button type="submit" class="btn btn-sm btn-outline-danger">
                      <i class="bi bi-trash"></i>
                    </button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } else { %>
      <p class="text-muted text-center py-4">No coordinators found.</p>
    <% } %>
  </div>
</div>
